kmt_claimed_state = {
	enable = { always = yes }

	icon = GFX_idea_kmt_claimed_state
	remove_trigger = {
    		NOT = {
       			OR = {
       				is_owned_by = GLA
          			is_owned_by = DET
        		}
    		}
	}
}

det_claimed_state = {
	enable = { always = yes }

	icon = GFX_idea_det_claimed_state
	remove_trigger = { 
		NOT = {
       			OR = {
       				is_owned_by = GLA
          			is_owned_by = KMT
        		}
    		}
	}
}

